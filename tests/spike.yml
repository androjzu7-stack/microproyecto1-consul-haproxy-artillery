config:
  target: "http://localhost:8080"
  phases:
    - duration: 10
      arrivalRate: 10
    - duration: 10
      arrivalRate: 80
    - duration: 10
      arrivalRate: 10
scenarios:
  - flow:
      - get:
          url: "/"
